apiVersion: v1
kind: ResourceQuota
metadata:
  name: audit-quota
  namespace: audit-project
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1"
    limits.memory: "1Gi"
    pods: "10"